[project]
name = "emissary_backend"
version = "0.1.0"
description = "Backend for the Emissary"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-generativeai>=0.7.2",
    "sunholo[gcp,http]>=0.110.2",
]
